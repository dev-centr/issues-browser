= Installer notes

Windows/installer packaging for **issues-browser** (formerly issue-tracker).

Build the CLI or GUI with Dub, then package the resulting `issues-browser` binary with your preferred installer toolchain.
See the main project README and docs for sync paths (`<repo>/.issues/database.sqlite`) and guardrails.
